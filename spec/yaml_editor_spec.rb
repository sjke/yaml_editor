require "spec_helper"

describe YamlEditor do
  it "has a version number" do
    expect(YamlEditor::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
